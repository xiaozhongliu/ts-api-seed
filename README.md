# ts-api-seed

> a typescript node api seed project.

## Build Setup

``` bash
# install dependencies
cnpm i

# compile in watch mode
npm run build

# serve in watch mode
npm start

# attach to debug
vscode -> F5
```